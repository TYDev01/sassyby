[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate"

[deploy]
startCommand = "until npx prisma migrate deploy; do echo 'DB not ready, retrying in 5s...'; sleep 5; done && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
